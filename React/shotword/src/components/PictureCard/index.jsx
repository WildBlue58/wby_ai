import './style.css'

const PictureCard = (props) => { 
    const {
        word,
        audio,
        uploadImg,
    } = props
    return (
        <div>
        PictureCard
        </div>
    )
}

export default PictureCard;